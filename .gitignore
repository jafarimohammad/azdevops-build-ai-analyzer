# build artifacts
node_modules/
*.vsix
*.rar

# local tooling / editor
.claude/
.vscode/
.DS_Store
Thumbs.db

# internal-only docs (contain the real internal LLM address — never publish)
docs-internal/
pat.txt
